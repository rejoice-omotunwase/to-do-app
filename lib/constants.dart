import 'package:flutter/material.dart';

const kListTextStyle = TextStyle(
  color: Colors.black,
  fontSize: 20.0,
);

const kBorderStyle = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.only(
    topLeft: Radius.circular(20.0),
    topRight: Radius.circular(20.0),
  ),
);
